# Legacy ML Artifacts

Historical ML artifacts retained for audit/backtesting should live here over
time. The existing `models/artifacts/entry_quality_v1.x` files are legacy
backtest-era artifacts and are not approved runtime artifacts.

Do not point `ML_MODEL_PATH` at legacy artifacts for live/shadow runtime use.
