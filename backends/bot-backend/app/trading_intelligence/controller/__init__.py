"""CATI orchestration boundary (Section 6.1). Interfaces here define WHERE
future orchestration happens; ``cati_controller.py`` is the current, shadow-
only implementation of the parts already built (Sections 9-13). It has no
order authority, no broker client, no capital or slot access."""
