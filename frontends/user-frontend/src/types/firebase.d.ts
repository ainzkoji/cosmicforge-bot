// Firebase is installed as a first-class dependency, so the SDK's bundled
// declarations should be used directly. This file is intentionally kept as a
// harmless placeholder for older imports that expected a local Firebase type shim.
